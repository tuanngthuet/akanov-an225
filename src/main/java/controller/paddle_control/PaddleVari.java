package controller.paddle_control;

public class PaddleVari {
    public static int PAD_HEIGHT = 20;

    // Values for the BASIC paddle
    public static int BASIC_PAD_WIDTH = 180;
    public static int BASIC_PAD_SPEED = 10;

    // Value for the BUFF paddle
    public static int BUFF_PAD_WIDTH = 200;

    // Values for the NERF paddle
    public static int NERF_PAD_WIDTH = 150;
    public static int NERF_PAD_SPEED = 15;

    // Value for the FROZEN paddle
    public static int FROZEN_PAD_SPEED = 0;
}
